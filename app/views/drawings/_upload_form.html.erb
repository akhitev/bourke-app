<h1>Upload new drawing</h1>
<%= form_tag({:action => :upload},:remote => true,:multipart => true) do %>
    <label for="file">Choose file to Upload</label><%= file_field_tag 'file' %>
    <%= submit_tag %>
<% end %>
